# Backend Configuration
PORT=8000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/octofit

# Codespaces Configuration (automatically set in Codespaces environment)
# CODESPACE_NAME is set automatically in Codespaces
# When available, API will use: https://$CODESPACE_NAME-8000.app.github.dev
# When not available, API uses: http://localhost:8000
